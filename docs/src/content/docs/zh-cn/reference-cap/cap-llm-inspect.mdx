---
title: cap_llm_inspect — 图像分析
description: 不依赖 Agent 运行时的独立本地图像推理能力。
---

import LinkToSource from '@/components/LinkToSource.astro';
import { Aside } from '@astrojs/starlight/components';

源码：<LinkToSource relativePath="components/claw_capabilities/cap_llm_inspect/src/cap_llm_inspect.c" /> · 头文件：<LinkToSource relativePath="components/claw_capabilities/cap_llm_inspect/include/cap_llm_inspect.h" />

## 定位

`cap_llm_inspect` 通过**由 Capability 自己持有的独立推理客户端**分析本地图像。它不依赖 `claw_core`，也不进入 Agent 请求循环。

它刻意只保留最小范围：
- 读取一个受支持的本地图像
- 发送一次图像与提示词请求
- 返回提供者的文本结果

## 工具接口

`cap_llm_inspect` 注册了单个 Callable `inspect_image`：

| 工具 ID | `inspect_image` |
| :-------:|----------------|
| 描述 | Analyze a local image from an absolute path. <br /> Confirm the path first, then provide a prompt describing what to inspect. |
| 输入 | `{ "path": "<绝对路径>", "prompt": "<分析提示>" }` |
| 输出 | LLM 对图像的文本描述 |

**典型调用场景：**

```d2
user: "用户"
cap_im: "cap_im_platform"
fatfs: "FATFS"
router: "Event Router"
inspect: "inspect_image\n(cap_llm_inspect)"
llm: "LLM\n多模态推理"
send: "(im)_send_message"

user -> cap_im: "发送图片"
cap_im -> fatfs: "下载保存"
cap_im -> router: "attachment_saved 事件"
router -> inspect: "call_cap\n(规则触发)"
inspect -> llm: "图像 + prompt"
llm -> inspect: "分析结果"
inspect -> send: "结果文本"
send -> user: "回复"
```

## 核心实现：独立图像推理

Capability 只拥有图像预处理、提供者请求和 HTTP 响应，核心逻辑：

```c
static const char *CAP_LLM_INSPECT_SYSTEM_PROMPT =
    "You analyze local image files for the ESP32 claw. "
    "Describe visible content plainly and briefly. "
    "If the image is unclear, say what is uncertain instead of guessing.";

static esp_err_t cap_llm_inspect_execute(const char *input_json,
                                          const claw_cap_call_context_t *ctx,
                                          char *output, size_t output_size)
{
    // 此处之前已解析并校验 path 和 prompt。
    char *analysis = NULL;
    char *error_message = NULL;

    xSemaphoreTake(s_runtime_lock, portMAX_DELAY);
    if (!s_runtime) {
        err = ESP_ERR_INVALID_STATE;
        error_message = strdup("LLM image inference is not configured");
    } else {
        err = cap_llm_inspect_runtime_infer_image(
            s_runtime,
            path_json->valuestring,
            CAP_LLM_INSPECT_SYSTEM_PROMPT,
            prompt_json->valuestring,
            &analysis,
            &error_message);
    }
    xSemaphoreGive(s_runtime_lock);
    snprintf(output, output_size, "%s", analysis);
    free(analysis);
}
```

### 与普通 LLM 对话的区别

`cap_llm_inspect` 的私有运行时与正在处理用户请求的 Agent 对话完全隔离：

- 使用专属的 `system_prompt`（专注于图像描述，不携带对话历史）
- 不读取或持久化当前 session 上下文
- 不触发 `cap_skill` 的工具可见性管理
- 支持图像作为输入（依赖底层 LLM 提供者的多模态能力）

### 错误处理

```c
if (err != ESP_OK) {
    snprintf(output, output_size,
             "Error: image analysis failed (%s)%s%s",
             esp_err_to_name(err),
             error_message ? ": " : "",
             error_message ? error_message : "");
    free(error_message);
    return err;
}
```

若底层 LLM 不支持多模态，或图像路径不存在，错误信息直接写入 `output` 返回给调用方（LLM 或自动化规则）。

## 设计原则分析

`cap_llm_inspect` 展示了几个重要设计选择：

1. **独立 system prompt**：图像分析不应受对话历史干扰，使用固定的专用提示词保证输出一致性

2. **路径确认原则**：描述中明确要求 "Confirm the path first"——LLM 在调用前应先通过 `cap_files` 的 `list_dir` 确认文件存在，避免因路径错误浪费 LLM 调用

3. **无 session 状态**：只保留复制后的提供者配置，每次图像请求彼此独立

4. **关注点分离**：IM 附件下载由 `cap_im_platform` 负责，路径管理由 `cap_files` 负责，分析逻辑集中在 `cap_llm_inspect`

## 刻意限制的范围

Capability 明确不包含：

- 音频或文档处理
- Agent 历史、工具循环或上下文持久化
- 流式响应、重试或通用提供者框架

<Aside type="tip">
  **为什么保持最小范围？**
  `inspect_image` 只需要一个本地图像和一个语义结果；额外的 Agent 运行时功能只会增加耦合，不能帮助它完成任务。
</Aside>
