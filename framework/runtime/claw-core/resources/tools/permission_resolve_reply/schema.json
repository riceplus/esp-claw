{
  "type": "function",
  "function": {
    "name": "permission_resolve_reply",
    "description": "Resolve a user's natural-language reply to one pending permission request.",
    "parameters": {
      "type": "object",
      "properties": {
        "decision": {
          "type": "string",
          "enum": ["yes", "no", "other"],
          "description": "yes for clear consent, no for clear refusal, and other for every reply that does not grant permission."
        },
        "reason": {
          "type": "string",
          "description": "Required when decision is other. Concisely explains why the reply does not grant permission."
        }
      },
      "required": ["decision"]
    }
  }
}
