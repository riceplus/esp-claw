{
  "type": "function",
  "function": {
    "name": "profile_replace",
    "description": "Replace one editable global profile document. Use this only for durable persona, assistant identity, or user-profile changes, not ordinary facts.",
    "parameters": {
      "type": "object",
      "properties": {
        "document": {
          "type": "string",
          "enum": ["soul", "assistant_identity", "user_profile"],
          "description": "The profile document to replace."
        },
        "content": {
          "type": "string",
          "description": "The complete replacement Markdown content for the selected document."
        }
      },
      "required": ["document", "content"]
    }
  }
}
