Revise a remembered fact with `memory_update`, identified by the `id` shown in
brackets by `memory_recall` / `memory_list`. Provide only the fields you want to
change — content, tags, or keywords; the rest stay as they were.
