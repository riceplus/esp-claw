{
  "type": "function",
  "function": {
    "name": "subagent_delete",
    "description": "Stop and remove an active subagent by id, including its entire subtree. Only subagents in your own subtree can be deleted.",
    "parameters": {
      "type": "object",
      "properties": {
        "agent": {
          "type": "string",
          "description": "The id of the subagent to delete (e.g. \"agent-7\"), as returned by subagent_spawn or subagent_list."
        }
      },
      "required": ["agent"]
    }
  }
}
