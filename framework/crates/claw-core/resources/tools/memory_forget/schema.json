{
  "type": "function",
  "function": {
    "name": "memory_forget",
    "description": "Forget a memory you no longer need, identified by its id from memory_recall or memory_list. This is permanent.",
    "parameters": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The id of the memory to forget (as shown in brackets by memory_recall/memory_list)."
        }
      },
      "required": ["id"]
    }
  }
}
