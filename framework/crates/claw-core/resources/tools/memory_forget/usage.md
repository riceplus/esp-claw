Forget a fact you no longer need with `memory_forget`, identified by the `id`
shown in brackets by `memory_recall` / `memory_list`. This is permanent, so
prefer `memory_update` when the fact merely changed.
