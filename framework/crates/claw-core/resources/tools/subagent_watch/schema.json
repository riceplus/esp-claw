{
  "type": "function",
  "function": {
    "name": "subagent_watch",
    "description": "Get the current state of an active subagent or a completed subagent whose detached result has not yet been accepted, including its kind, depth, and status.",
    "parameters": {
      "type": "object",
      "properties": {
        "agent": {
          "type": "string",
          "description": "The id of the subagent to inspect (e.g. \"agent-7\"), as returned by subagent_spawn or subagent_list."
        }
      },
      "required": ["agent"]
    }
  }
}
