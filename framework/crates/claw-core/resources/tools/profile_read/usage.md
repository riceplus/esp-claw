Read a global profile document with `profile_read`. Use `soul` for behavior and
style principles, `assistant_identity` for the assistant/device role and
boundaries, and `user_profile` for stable user preferences and interaction
agreements.
