{
  "type": "function",
  "function": {
    "name": "profile_read",
    "description": "Read one editable global profile document: assistant soul, assistant identity, or user profile.",
    "parameters": {
      "type": "object",
      "properties": {
        "document": {
          "type": "string",
          "enum": ["soul", "assistant_identity", "user_profile"],
          "description": "The profile document to read."
        }
      },
      "required": ["document"]
    }
  }
}
