Use `outcome: "execute"` after semantic user approval and include the full final `plan`.
Use `outcome: "cancel"` when the user abandons the plan; omit `plan` and optionally include a closing `message`.
