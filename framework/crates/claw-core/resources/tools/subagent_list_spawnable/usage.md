List the kinds of subagent you may spawn, each with a one-line description of its
role. Use it to pick the right `kind` for `subagent_spawn` instead of guessing —
the list reflects your own spawn policy, so anything it returns is permitted.
