{
  "type": "function",
  "function": {
    "name": "memory_list",
    "description": "List the durable facts you currently remember, newest first. Use this to review what is in memory before editing or forgetting.",
    "parameters": {
      "type": "object",
      "properties": {
        "limit": {
          "type": "integer",
          "minimum": 1,
          "description": "Maximum number of memories to return."
        }
      }
    }
  }
}
