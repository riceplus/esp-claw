List everything you currently remember with `memory_list`, newest first. Each
entry shows its id in brackets so you can pass it to `memory_update` or
`memory_forget`. Prefer `memory_recall` when you already know the topic.
